# ---------------------------------------------------------------------------
# Frontend env — ONLY needed for split local dev (frontend on :3000, backend
# on :4000). In the unified build the frontend is served by the backend and
# calls the API at the same origin (/api/v1), so this is not required.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
